Items are to be deleted after completion

* Fix 'Categorii' page to only display an index-like list.
* ? Implement an authors page
* Ensure phone compatibility
* Finish new template
* Make favicon
* Add images to some recipes

